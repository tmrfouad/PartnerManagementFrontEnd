export interface ActionTypeComment {
        attendee?: string[];
        where?: string;
        visitReason?: string;
        comment?: string;
        actionWhen?: string;
        requiredActions?: string;
        callSummary?: string;
}
