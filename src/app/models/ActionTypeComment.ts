export interface ActionTypeComment {
        attende?: string[];
        where?: string;
        visitReason?: string;
        comment?: string;
        actionWhen?: string;
        requiredActions?: string;
}
